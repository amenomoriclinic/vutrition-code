# CLAUDE.md

個人用の栄養管理アプリ（Next.js 16 App Router + React 19、Supabase、Vercel）。画面はほぼ `app/page.tsx` 1ファイル。

## 作業の進め方

- 作業は必ず main からブランチを作って行う。main に直接コミットしない。
- main へのマージと push は、ユーザーの確認を得てから行う。
- 内容ごとにコミットを分ける。

## 開発サーバーとビルド

- ユーザーは普段 `next dev`（ポート 3000）を動かしている。**動いている間は `next build` を実行しない**（`.next` を上書きして開発サーバーが壊れる）。
- ビルドや動作の確認は、別フォルダの git worktree（例: スクラッチ領域）で行い、別ポート（3100 など）を使う。
  - 依存パッケージを変えない確認なら、worktree の `node_modules` に本体の `node_modules` へのジャンクションを張ってよい。**worktree を削除する前に、必ずジャンクションだけを先に外す**（`cmd /c rmdir <worktree>\node_modules`）。そのまま `git worktree remove --force` すると本体の `node_modules` が消えるおそれがある。
  - 依存パッケージを変える作業（`npm install`）は、動いている開発サーバーの足元を変えないよう、worktree 側で独立に `npm install` して行う。
- 型チェックは `npx tsc --noEmit -p .` で行える（開発サーバーと干渉しない）。
- `next-env.d.ts` は `next dev` / `next build` が毎回書き換えるため Git の管理外。

## データとテスト

- ローカルの開発環境も**本番の Supabase** につながっている。確認はテスト用のデータ（例: 「テスト 100g」の定番食品）で行い、既存の記録・プリセットを変更・削除しない。
- テーブルは RLS 有効・anon に全操作を許可するポリシー（ログインなし、1人で使う前提）。テーブル定義は `db/supabase_create_table.sql`。
- Claude API を呼ぶ `/api/vision`・`/api/weekly-summary` は利用料がかかる。画面の確認ではブラウザ側で応答を差し替えて呼ばない。

## Vercel

- 環境変数は **Production のみ**に設定されている。`vercel env pull` には `--environment=production` が必要。
- Node.js は 24.x（Next 16 は 20.9 以上が必要）。

## 推奨値の計算（`lib/dri.ts`、表は `data/dri-2025.json`）

ホーム・集計・グラフ・先週サマリー（AI 分析）・設定は、すべて `calcRequirements()` の同じ値を使う。

- **出典**：日本人の食事摂取基準（2025年版）策定検討会報告書（厚生労働省）。ページ番号は `data/dri-2025.json` の `_source` に記載。
- **基礎代謝量**：国立健康・栄養研究所の式（Ganpule AA ほか, Eur J Clin Nutr 2007;61:1256-61。報告書 エネルギー 表2 p.65）
  `(0.0481×体重kg + 0.0234×身長cm − 0.0138×年齢 − 定数) × 1000 ÷ 4.186`、定数は男性 0.4235・女性 0.9708。
- **エネルギー必要量** ＝ 基礎代謝量 × 身体活動レベル（報告書 表4 p.67、年齢別）＋ その日の運動（正味）。
  - 身体活動レベルの初期値は「低い」。説明文は表5 p.68（75歳以上は参考表2 p.78 の注）。75歳以上に「高い」はない。
  - 先週サマリーでは、週の運動（正味）の合計 ÷ 7 を加える。
  - 参考表2の固定値（2,750 など）は使わない。
- **運動は正味の値**（安静時の 1 MET 分を除く）を記録・加算する。
  - METs：`(MET − 1) × 体重 × 時間`
  - ランニング：`体重 × 距離km × 1.0`（ACSM の代謝式、正味 0.2 mL/kg/m を 5 kcal/L で換算）
- **P・F・C**：合計がエネルギーと一致するように決める。
  - たんぱく質：目標量（%エネルギー）の中間値（18〜49歳 16.5%、50〜64歳 17%、65歳以上 17.5%）。推奨量（g）は下回っていないかの確認用に別に表示。
  - 脂質：25%
  - 炭水化物：残り
- **食塩相当量**：目標量 男性 7.5g未満・女性 6.5g未満（18歳以上は全年齢共通、報告書 ナトリウム 表1 p.248）。
- 18歳未満は対象外（推奨値を出さず理由を表示）。80歳以上は式の妥当性が確認された年齢幅（18〜79歳）外として注意を表示。
- 使う体重は健康記録の最新値（なければプロフィールの手入力値）、身長は保存済みの値。

## 吸収リン

- 現在の「目安上限 男性400mg・女性300mg」には出典がない。**「傾向を見る指標」として扱う方針**で、今後作り直す予定（別ブランチで行う）。推奨値の計算とは切り離しておく。

## 今後の予定

- ログインと RLS によるデータ保護（プロフィールの Supabase 移行と、年齢の代わりの生年月日入力を含む。プロフィールは今は端末ごとの localStorage）
- 吸収リンの作り直し
- Claude のモデルの更新（`claude-opus-4-5` のまま）と API キーの整理
